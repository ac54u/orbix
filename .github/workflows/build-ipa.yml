name: Build Orbix IPA

on:
  push:
    branches:
      - master
      - main
    tags:
      - 'v*'
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: write

jobs:
  build-ios:
    runs-on: macos-15

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v7

      - name: Install XcodeGen
        env:
          HOMEBREW_NO_REQUIRE_TAP_TRUST: 1
        run: brew install xcodegen

      - name: Generate Xcode Project
        run: xcodegen generate

      - name: Run Tests
        run: |
          set -o pipefail
          xcodebuild test \
            -project Orbix.xcodeproj \
            -scheme Orbix \
            -destination 'platform=iOS Simulator,name=iPhone 16' \
            -derivedDataPath build 2>&1 | tee test.log | xcpretty --simple --color || {
              echo "Tests failed. Tail:"
              tail -n 100 test.log
              exit 1
            }

      - name: Build iOS (Unsigned)
        run: |
          set -o pipefail
          xcodebuild build \
            -project Orbix.xcodeproj \
            -scheme Orbix \
            -sdk iphoneos \
            -configuration Release \
            CODE_SIGN_IDENTITY="" \
            CODE_SIGNING_ALLOWED="NO" \
            CODE_SIGNING_REQUIRED="NO" \
            -derivedDataPath build 2>&1 | tee build.log | xcpretty --simple --color || {
              echo "Build failed. Full log:"
              tail -n 100 build.log
              exit 1
            }

      - name: Package Unsigned IPA
        run: |
          mkdir -p Payload
          cp -r "build/Build/Products/Release-iphoneos/Orbix.app" Payload/
          zip -r Orbix.ipa Payload/
          rm -rf Payload

      - name: Upload IPA Artifact
        uses: actions/upload-artifact@v7
        with:
          name: Orbix-Unsigned-IPA
          path: Orbix.ipa
          retention-days: 7

      - name: Publish Release (on tag)
        if: startsWith(github.ref, 'refs/tags/v')
        uses: softprops/action-gh-release@v3
        with:
          files: Orbix.ipa
          generate_release_notes: true
